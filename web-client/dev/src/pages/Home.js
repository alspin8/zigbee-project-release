import React from 'react';
import DataTable from '../component/Table';

const Home = () => {
    return (
        <div style={{padding: '10px'}}>
            <DataTable />
        </div>
    );
};

export default Home;